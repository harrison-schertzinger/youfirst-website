# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
public/images/20250812_C6227.MP4
parent-emails.csv

# Local-only PII and generated outputs
/email-lists/
/scripts/seed-data.json
/scripts/gen-link.ts

# Separate Node tooling sub-project — own package.json, node_modules,
# raw photos, generated PNGs. Don't track from this repo.
/scripts/recruiting-graphics/

# Local dry-run renders — contain guardian emails and balances. Never commit.
tmp/

# Generated from documents/club-standard/content.mjs by
# scripts/build-club-standard.mjs. The PDFs in public/documents/ ARE tracked —
# they get attached to receipts — but the intermediate HTML is not.
/documents/club-standard/build/

# Output of scripts/preview-placement-email.ts. Renders a real athlete's merge
# values when run with --live, so it stays local.
/documents/placement-preview/
